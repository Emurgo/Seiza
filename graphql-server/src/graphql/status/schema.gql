enum CurrencyEnum {
  USD
  EUR
  JPY
}

# Not sure if Status is a good name
type Status {
  epochNumber: Int
  blockCount: Int
  decentralization: Float # todo scalar fraction
  price(currency: CurrencyEnum!): Float
  stakePoolCount: Int
}

type Query {
  currentStatus: Status
}

